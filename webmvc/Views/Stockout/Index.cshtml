@model IEnumerable<webmvc.Models.Buoi12.Stockout>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.OrderTime)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.OrderTime)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.StockoutId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.StockoutId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.StockoutId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
